#include "main.h"
#include "../0x01-variables_if_else_while/0-positive_or_negative.c"

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
    int i;

    i = 0;
    positive_or_negative(i);

    return (0);
}
